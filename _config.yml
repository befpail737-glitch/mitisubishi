# Cloudflare Pages Configuration
# https://pages.cloudflare.com/

# Build settings
build:
  command: echo "Build not required for static site"
  output: .

# Environment variables
environment:
  NODE_VERSION: "18"

# Redirects (optional)
redirects:
  - from: /404
    to: /404.html
    status: 404
